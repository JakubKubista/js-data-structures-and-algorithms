"use strict";

/**
 * Clone a linked list with next and random pointer.
 * Time Complexity: O(n)
 */

class Node {
    constructor(data, next = null) {
        this.data = data,
            this.next = next
    }
}